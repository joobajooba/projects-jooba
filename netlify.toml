# Netlify configuration for projects-jooba
# Publish directory must match Netlify UI (Build settings → Publish directory).
# This repo uses "public" so the UI and config agree.

[build]
  publish = "public"
  command = ""
