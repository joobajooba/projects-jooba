# Netlify configuration for projects-jooba
# https://docs.netlify.com/configure-builds/file-based-configuration/
#
# In Netlify UI: Site configuration → Build & deploy → Build settings
# Set "Publish directory" to a single dot: .   (or leave blank to use this file)

[build]
  publish = "."
  command = ""
