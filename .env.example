# WalletConnect Configuration
# Get a free project ID at https://cloud.reown.com/
# After creating a project:
# 1. Copy your Project ID
# 2. Add your domain (j00ba.xyz) to the Domain allowlist in project settings
# 3. Add this as VITE_WALLETCONNECT_PROJECT_ID in Netlify (Build & deploy → Environment).
VITE_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Supabase Configuration
# Get your anon key from https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=https://jitkwbatwymqtlzxiyil.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Alchemy API Keys (Optional - for NFT fetching)
# Get free API keys at https://www.alchemy.com/
# You can use one general key for all networks, or separate keys per network
# If not provided, the app will use OpenSea API (rate limited) or ApeScan for ApeChain

# General Alchemy API key (works for all supported networks)
VITE_ALCHEMY_API_KEY=your_alchemy_api_key_here

# Ethereum-specific API key (optional - overrides general key for Ethereum)
VITE_ALCHEMY_API_KEY_ETH=your_ethereum_alchemy_api_key_here

# ApeChain-specific API key (optional - overrides general key for ApeChain)
# Note: Alchemy may not support ApeChain yet, so ApeScan API will be used as fallback
VITE_ALCHEMY_API_KEY_APECHAIN=your_apechain_alchemy_api_key_here
